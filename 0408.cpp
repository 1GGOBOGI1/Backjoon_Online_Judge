#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int C;
    cin >> C;
    while (C--)
    {
        vector<string> ans;
        string W;
        cin >> W;
        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            string input;
            cin >> input;

            //정답인지 확인
            
        }

        for (int i = 0; i < ans.size(); i++)
            cout << ans[i] << endl;
    }
}